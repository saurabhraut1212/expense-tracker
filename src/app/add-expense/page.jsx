import React from 'react';
import AddExpensePage from '../../pages-section/add-expense';

export const metadata = {
	title: 'Expense Form',
	description: 'Created For People who want to keep track on their expense',
};

const IndexPage = () => {
	return <AddExpensePage />;
};

export default IndexPage;
