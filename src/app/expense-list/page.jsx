import React from 'react';
import ExpenseListPage from '../../pages-section/expense-list';

export const metadata = {
	title: 'Expense List',
	description: 'Created For People who want to keep track on their expense',
};

const IndexPage = () => {
	return <ExpenseListPage />;
};

export default IndexPage;
