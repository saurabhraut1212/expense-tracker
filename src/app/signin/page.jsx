import React from 'react';
import SignInPage from '../../pages-section/signin';

export const metadata = {
	title: 'SignIn page',
	description: 'Created For People who want to keep track on their expense',
};

const IndexPage = () => {
	return <SignInPage />;
};

export default IndexPage;
