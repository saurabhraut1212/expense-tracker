import React from 'react';
import SignUpPage from '../../pages-section/signup';

export const metadata = {
	title: 'SignUp Page',
	description: 'Created For People who want to keep track on their expense',
};

const IndexPage = () => {
	return <SignUpPage />;
};

export default IndexPage;
